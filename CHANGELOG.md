# 0.6.0

- 依 Counter、Feedback、Geolocation feature 重整目錄；頁面只保留布局，互動 hook、服務、型別與工具各自分層。
- 拆分超過 200 行的 CounterPage、CounterHeader 與 FeedbackSettingsPage；目前所有 TSX 均低於 200 行。
- GPS 改為獨立排程取樣：每分鐘切成六個固定 10 秒視窗，各視窗最多請求一次，不再持續監聽。
- 每個 GPS 取樣窗使用節流與 in-flight 防重入；分頁在背景時停止排程，回到前景不補跑錯過的視窗。
- 計數或儲存時使用最近一次成功定位；若定位已超過 20 秒，就不附帶 GPS 資料。按壓車種按鈕不會觸發新的定位請求。
- 車種按鈕維持無固定冷卻時間；260ms 僅為視覺回饋，不阻擋快速連續計數。

# 0.5.0

- 車種由六種擴充為七種，新增「自行車」，排序位於「其他」之前；同步補齊 10 款主題配色與七車種版面。
- 漢堡選單改為由右側滑出的 Sheet。
- 右側 Sheet 新增 Auto Save 開關；啟用後每 15 分鐘檢查，工作區不為 0 時執行保存。
- 頁首 新增 Quick Save。
- Quick Save／Auto Save 依序執行：寫入含工作區快照的 `pending` Save 事件、下載主畫面 PNG、標記保存完成，再將主畫面工作區計數歸零。
- 全期間累計與原始事件不因 Save 歸零；原始資料新增 `eventType`、`saveType`、`saveId`、截圖檔名、保存時工作區快照與本次保存事件數。
- 匯出頁不再把 Count／Save 混在同一張稀疏表格，改為可獨立查看與匯出的「計數明細」及「保存紀錄」；保存快照拆成七個車種欄位。
- Auto Save 截圖失敗時不清零，並關閉 Auto Save，避免瀏覽器持續重試自動下載。
- 修正 Quick Save 截圖使用 `blob:` SVG 時污染 Canvas、造成 PNG 匯出失敗且主畫面無法歸零的問題。
- 修正 LocalStorage 寫入失敗時 Save 卡住、失敗 Save 導致重試事件數低估，以及快速連續點擊可能使用舊計數的問題。
- 應用程式重新載入時，未完成的 `pending` Save 會恢復為 `failed`，並保留工作區數字。
- 定位監聽前先查詢瀏覽器權限；已封鎖時不再重複呼叫 Geolocation API，避免 Strict Mode 造成重複權限警告。
- 新增 `public-meta/README.md`，記錄本次未附上的 `public/` 資產名稱、位置與用途。

# 0.4.0

- 移除橫向滑卡、單車種聚焦、底部拇指操作與小螢幕交錯列，保留 10 款主題。
- 新增獨立 `#/feedback` 震動與音效設定頁，不受主題切換影響。
- 計數增加、計數減少、小於 0 錯誤可各自設定震動等級與音效。
- 震動提供關閉、輕微、中等、強烈兩段；音效提供關閉、短促點擊、單聲提示、雙音確認、低頻警告。
- 設定頁可立即測試，並顯示瀏覽器的 Web Vibration／Web Audio 支援狀態。
- 舊版若保存了已刪除主題，啟動時自動遷移到標準藍白，不會造成主題查找錯誤。

# 0.3.0

- 修正黑白工程、清單大字主題的漢堡按鈕對比。
- 將計數 Card 圓角與 MUI 全域 shape 分離；Menu／Dialog／Button 使用固定小圓角。
- 所有計數 Card 最大圓角收斂至 12px。
- 新增非對稱資訊牆與錯位鍵盤。
- 新增小螢幕交錯列。
- 寬度 ≤370px 或高度 ≤680px 時啟用緊湊間距；主題選擇器寬度 ≤360px 改單欄。
